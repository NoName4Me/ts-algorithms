import Sort, { CallBack } from "../Sort";

export default class RadixSort<T> extends Sort<T> {
  constructor(callback?:CallBack) {
    super(callback);
  }

  sort(origianlArray:T[]):T[] {
    for(let i)
    return;
  }
}