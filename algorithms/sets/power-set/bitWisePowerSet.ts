export default function bitWisePowerSet(origin: []) {
  const subSets: [] = [];
  const N = 2 ** origin.length;
  for (let i: number = 0; i < N; i++) {
    for (let j: number = 0; j < origin.length; j++) {
      
    }
  }
}